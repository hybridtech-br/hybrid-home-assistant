# Architecture

A direção de dependência é:

Applications → Intelligence → Knowledge → Digital Twin → Runtime → Kernel → Foundation

A entrega atual implementa apenas uma fração do Foundation. Nenhuma outra camada está funcional.
