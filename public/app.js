const $=s=>document.querySelector(s);const $$=s=>[...document.querySelectorAll(s)];let data=null;const api=async(url,opt={})=>{const r=await fetch(url,{headers:{'Content-Type':'application/json'},...opt});if(!r.ok)throw new Error((await r.json()).error||'Erro');return r.json()};function statusText(d){if('on'in d.state)return d.state.on?'Ligado':'Desligado';if('live'in d.state)return d.state.live?'Ao vivo':'Offline';if('open'in d.state)return d.state.open?'Aberto':'Fechado';if('temperature'in d.state)return `${d.state.temperature}°C`;return d.state.lastMotion||'Online'}function render(){if(!data)return;const{home,rooms,scenes,devices,totals}=data;$('#heroTitle').textContent=`Bom dia, Administrador!`;$('#heroSubtitle').textContent=`${home.name} está conectada e segura.`;$('#stats').innerHTML=[['Dispositivos',totals.devices,'◫','blue'],['Consumo hoje',`${home.consumption} kWh`,'ϟ','green'],['Segurança',home.security,'⬡','purple'],['Temperatura',`${home.temperature}°C`,'◉','orange']].map(([l,v,i,c])=>`<article class="stat-card"><span class="stat-icon ${c}">${i}</span><div><small>${l}</small><strong>${v}</strong></div></article>`).join('');$('#roomsGrid').innerHTML=rooms.map(r=>`<article